## Setup
Create virtual env:
```bash
# Linux / Mac:
python3 -m venv .

# Windows:
python -m venv .
```

Activate virtual env:
```bash
# Linux / Mac:
source bin/activate

# Windows:
.\bin\Activate.ps1
```

Install dependencies:
```bash
pip install -r requirements.txt
```
